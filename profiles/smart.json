{
  "name": "smart",
  "description": "Full capability (138 agents, 296 skills - identical to all, nothing disabled) with token optimization. Reduced plugin injection budgets via settings.json env vars. Target: 30-35% lower session-start tokens vs all.",
  "agents": "all",
  "skills": "all",
  "rules": "all",
  "optimizations": {
    "userPromptSubmitHooks": 3,
    "sessionBannerLazy": true,
    "vercelPluginBudget": {
      "injection": 6000,
      "promptInjection": 3000,
      "lexicalPrompt": "off"
    }
  }
}
