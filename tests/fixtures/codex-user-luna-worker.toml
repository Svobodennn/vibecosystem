name = "luna_worker"
description = "User-owned reserved worker fixture."
developer_instructions = "This sentinel must survive mirror installation unchanged."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
