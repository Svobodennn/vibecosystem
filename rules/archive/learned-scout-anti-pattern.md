# Learned: scout:anti-pattern

> Bu kural otomatik olusturuldu (10 tekrar, agent_learning tipi).

## Pattern
scout:anti-pattern

## Ornekler
- arrives. | **MUST ADD** to `AuthUserDto` + `normalizeAuthUser` + `User` |
- pass it. The backend controller also ignores it; `moderator.moderateText()` always posts to `textMod
- requested public), `'pending'` (submitted, awaiting review), `'approved'`, `'rejected'`

## Ilk gorulme
2026-06-11T12:17:51.540Z

## Son gorulme
2026-06-18T08:24:13.559Z
